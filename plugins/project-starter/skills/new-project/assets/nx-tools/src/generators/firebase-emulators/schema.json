{
  "$schema": "https://json-schema.org/draft-07/schema",
  "$id": "BeSpunkyFirebaseEmulators",
  "title": "Scaffold Firebase emulator config + Nx targets + app initialization",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "Name of the Angular app to wire (firebase.config.ts is generated under its src/app, and its serve target is wrapped to start the emulator suite).",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Which app?"
    },
    "workspaceName": {
      "type": "string",
      "description": "Workspace name used to build the FALLBACK offline emulator project id (`demo-<name>`). tools/emulators.sh normally DERIVES its --project from the app's environment.ts (single source of truth); this is the fallback when no env file is found, and the demo id seeded into environment.ts / firebase.config.ts. Defaults to the project name. (Not written to .firebaserc \u2014 that's the Firebase CLI's responsibility.)"
    },
    "staging": {
      "type": "boolean",
      "description": "Opt-in: also scaffold a first-class STAGING environment \u2014 environment.staging.ts (write-if-absent), a `staging` build configuration (swaps that env file in), and apphosting.staging.yaml (builds `--configuration=staging`) \u2014 so the workflow's `staging` App Hosting backend gets its own config/database instead of silently building prod's. Pairs with the `databaseId` field for same-project Firestore isolation. Default false.",
      "default": false
    },
    "wireProviders": {
      "type": "boolean",
      "default": false,
      "description": "Wire this layer's provider into the app's app.config.ts. TRUE ONLY WHEN ENSURING the layer (creating the capability) \u2014 app.config.ts is seeded, never owned, so a detect-only sync must not write to it. Any later provider change ships as a migration. Default false."
    }
  },
  "required": [
    "project"
  ]
}
