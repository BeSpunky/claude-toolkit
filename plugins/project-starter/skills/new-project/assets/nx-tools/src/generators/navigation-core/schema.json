{
  "$schema": "https://json-schema.org/draft-07/schema",
  "$id": "BeSpunkyNavigationCore",
  "title": "Scaffold the self-contained navigation-core kernel (navigation-x vendored + BeSpunky reusable additions)",
  "type": "object",
  "properties": {
    "directory": {
      "type": "string",
      "description": "Where to write the kernel source (an index.ts + lib/ are created inside). Defaults to libs/navigation-core/src."
    }
  }
}
