# Baton — 2026-08-06T22:00Z

**Status: the effort is FINISHED and waiting on one thing — the user's merge signal.** Nothing is
in flight. If you are picking this up cold, you do not need to build anything.

## Where it stands

Branch `feat/devcontainer-features`, worktree `.claude/worktrees/devcontainer-features`, 11 commits,
working tree clean, already rebased onto current `development` (`df6e521`).

**What it ships:** one local devcontainer feature, `bespunky-house-setup`, that installs nothing — its
whole content is a `postCreateCommand` chaining `.devcontainer/post-create.bespunky.sh` when that file
exists, so *adopted* projects finally run house setup. Plus the `mergeIntoExisting` fix it depends on
(an empty-container guard was silently dropping every house feature in a project with its own `features`
map), implemented as a named policy so a feature version pin survives. Versions: `0.32.0`, plugin and
payload.

**Read `DECISION.md` before touching anything.** §1–§8 are a design that was built and then *reverted*
after a five-agent adversarial review; §9 is what actually shipped and why the first attempt failed. The
banner at the top says so. Do not resurrect §1–§8.

## If the user says "done / verified"

Follow `bespunky-workflow:branch-and-release`: `git rebase development` and re-verify first (development
has moved once already during this effort, and took the `0.31.0`/`0.31.1` version numbers this branch
originally claimed — that is why it is `0.32.0`), then `git switch development && git merge --no-ff
feat/devcontainer-features`, then tear the worktree down. Stamp `DECISION.md` with `status: concluded`
frontmatter at that gate — it does not have it yet, deliberately, because the effort has not been
promoted.

## Loose ends the user was told about

- `backup/devcontainer-features-prerebase` — a safety ref pointing at the pre-rebase history. Delete
  whenever they are satisfied; it is not needed.
- **Found in passing, NOT fixed (unrelated, wants its own worktree):**
  `src/generators/shared-browser/shared-browser.tpl`'s `preflight_deps` failure message carries a
  hand-typed, already-drifted copy of the apt list — it names `util-linux` (never installed) and omits
  `procps`, `tmux` and both font packages. Same drift class as the `serve` executor fix at `dae4194`.

## How to re-verify

```
node tools/check-release-invariants/check.mjs
node tools/check-script-modes/check.mjs
bash tools/test-scaffold/run.sh
```

The generator/feature end-to-end harness is scratch, not committed:
`<scratchpad>/verify-chain.mjs`. It stages the payload, transpiles it with Node's built-in type
stripper (this repo has no `typescript` and no network), runs the real generator against Nx `FsTree`s,
and then **executes** the feature's declared `postCreateCommand` under `/bin/sh` — owned no-ops,
adopted runs the parked script, a failing script warns at rc 0, a version pin survives. If it is gone,
that is fine; it is evidence, not a regression test. A permanent generator-level harness does not exist
and is named in `DECISION.md` §9 as deliberately out of scope.
